#include <stdio.h>

int main(void){
    char name[10];
    char email[20];
    scanf("%s",name);
    printf("name: %s\n",name);
    scanf("%s",email);
    printf("email: %s\n",email);
    
    return 0;
}
